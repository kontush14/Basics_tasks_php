<?php
     //Conditions

     /*
     1.Check if the $test variable is greater than 10.

     $test = 11;
     if($test > 10)
     {
          echo '$test > 10';
     }
     else
     {
          echo '$test < 10';
     }
     */ 

     /*
     2.Check if the $test variable is greater than or equal to 10.

     $test = 10;
     if($test >= 10)
     {
          echo '$test >= 10';
     }
     else
     {
          echo '$test <= 10';
     }
     */

     /*
     3.Check that the $test variable is equal to 10.

     $test = 1;
     if($test == 10)
     {
          echo '$test == 10';
     }
     else
     {
          echo '$test != 10';
     }
     */

     /*
     4.Variables $test1 and $test2 are given. Check that the value of which of these variables
     is greater and display the corresponding message on the screen.
     
     $test1 = 4;
     $test2 = 3;
     if($test1 > $test2)
     {
          echo '$test1 > $test2';
     }
     else
     {
          echo '$test1 < $test2';
     }
     */

     /*
     5.Check if $num is greater than zero and less than 5.

     $num = 4;
     if($num > 0 && $num < 5)
     {
          echo 'Yes';
     }
     else
     {
          echo 'No';
     }
     */

     /*
     6.Check if $num is greater than or equal to 10 and less than or equal to 20.

     $num = 15;
     if($num >= 10 && $num <= 20)
     {
          echo 'Yes';
     }
     else
     {
          echo 'No';
     }
     */

     /*
     7.Check that the $test variable is true.

     $test = true;
     if($test === true)
     {
          echo 'Yes';
     }
     else
     {
          echo 'No';
     }
     */

     /*
     8.The variable $day contains some number from the interval from 1 to 31. 
     Determine which decade of the month this number falls into.

     $day = 25;
     if($day > 0 && $day <= 10)
     {
          echo '1 decade';
     }
     elseif($day > 10 && $day <= 20)
     {
          echo '2 decade';
     }
     else
     {
          echo '3 decade';
     }
     */

     /*
     9.Modify the previous task so that if the $day variable is not a number between 1 and 31, 
     an error message will be generated.

     $day = 31;
     if($day > 0 && $day <=31)
     {
          if($day > 0 && $day <= 10)
          {
               echo '1 decade';
          }
          elseif($day > 10 && $day <= 20)
          {
               echo '2 decade';
          }
          else
          {
               echo '3 decade';
          }
     }
     else
     {
          echo 'Enter a valid number';
     }
     */
?>